; [MASTER]
; load-plugins=pylint-custom

; [MESSAGES CONTROL]

; disable=all
; enable=
;     sensitive-data-checker,
;     f-string-checker,
;     line-continuation-checker,
;     range-iteration-checker

[MAIN]
load-plugins=continuation_checker

[MESSAGES CONTROL]

; disable=all
; enable=the-shenk-checker
enable=line-continuation-checker
    ; f-string-checker,
    ; line-continuation-checker,
    ; range-iteration-checker